name: scsmm
description: A sample command-line application with basic argument parsing.
version: 0.0.1

environment:
  sdk: ^3.5.0

executables: 
  scsmm: bin/scsmm.dart

dependencies:
  args: ^2.5.0
  path: ^1.9.0
  yaml: ^3.1.2
  yaml_edit: ^2.2.1

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
